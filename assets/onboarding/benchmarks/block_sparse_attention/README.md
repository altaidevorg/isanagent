# Block-sparse attention slice (Phase 4)

Exercise `PrefetchScalarGridSpec` mutations for non-contiguous block fetches.

Reference: Hugging Face block-sparse attention with JAX/Pallas blog patterns in conversion guide.

Evolution mutation class: `sparsity`.
